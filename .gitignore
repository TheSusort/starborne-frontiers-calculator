node_modules
.env
.DS_Store
*.log
dist
.worktrees/
supabase/.temp/
.superpowers/
docs/
.claude/
video/

# husky internals (generated; symlinked in worktrees)
.husky/_

# local editor/backup artifacts (never commit)
.env.bak
.cursor/

# database dumps — `supabase db dump` writes these to the repo root, and data.sql
# carries auth.users (every email and password hash) plus the whole inventory.
# Committed migrations live in supabase/migrations/, which is not matched here.
/*.sql
data.sql
schema.sql
roles.sql
