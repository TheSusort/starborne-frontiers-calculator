import React from 'react';

// Stub — Task 22 will replace this with the full implementation.
export const RenameAltModal: React.FC<{
    altId: string;
    currentUsername: string;
    onClose: () => void;
}> = () => null;
