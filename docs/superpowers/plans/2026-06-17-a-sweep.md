# Sub-project A — Cleanup Sweep (A.1/A.2/A.3) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close sub-project A by retiring A2-era transitional scaffolding — delete the dead legacy non-walked-team engine branch (A.3), collapse the live-OR-static debuff-landing dual-source into a single producer (A.2), and unify the duplicate live-landing read in triggers.ts (A.1) — all **byte-identically**.

**Architecture:** Three independent-ish cleanups, sequenced A.3 → A.2 → A.1 (A.2 removes a fallback consumer that A.1 then simplifies). No new behavior; the only permitted test changes are 4 fixture conversions in `triggers.test.ts` (each hand-verified to reproduce its prior landing outcome).

**Tech Stack:** TypeScript, Vitest. Combat engine in `src/utils/combat/`.

**Spec:** `docs/superpowers/specs/2026-06-17-a-sweep-design.md`. Epic: `docs/superpowers/specs/2026-06-17-combat-realism-epic-roadmap.md`.

**Golden gate:** **BYTE-IDENTICAL.** No combat `.snap` may move. The 4 `triggers.test.ts` fixture conversions (A.2) keep their exact assertions (same lands/resists counts). Never blind `vitest -u`.

**Workflow:** Main checkout, branch `feat/combat-sim-phase5-pr2` (no fresh worktree — esbuild crash). `gh auth switch --hostname github.com --user TheSusort` only for PR ops. Docs gitignored → `git add -f` + `--no-verify`.

**Test runner:** NEVER bare `npm test` (Vitest WATCH — hangs). Use `npx vitest run <name>` / `npx vitest run`; `npx tsc --noEmit`; `npm run lint` (max-warnings 0, EVERY task); `npm run audit:skills`.

---

## Design note (read first)

**B-items are already correct — DO NOT touch them** (audit, recorded in spec §1): `damageReduction` (derived from defence, no buff corpus), `healModifier` (folds via `outgoingHeal`/`incomingHeal`), the affinity-disadvantage rule on recurring/aura debuffs (`resolveEnemyDebuffs` already enforces it), and the canonical-stat routing. This sweep is pure scaffolding cleanup.

**A.2 default placement (load-bearing correctness point):** the `?? 200`/`?? 100` default for base-less actors MUST live inside `liveDebuffLandingChance`, NOT in `effectiveStatsOf` (which coerces a missing base to `0` for ALL readers — changing that ripples to attack/defence/etc.). Reproduce the fold with the 200/100 default by reading the raw base + the folded buff total directly (see Task 2).

---

## Task 1 (A.3): Delete the dead legacy non-walked-team engine branch

**Files:**
- Modify: `src/utils/combat/engine.ts` — the `else if (actor.kind === 'team')` legacy branch (~3257-3335) incl. the `sourceFired` landing-hook carve-out (~3287-3295).

**No new test** — this is dead-code removal; the gate is the full suite staying green + zero `.snap`.

- [ ] **Step 1: Confirm the branch is dead before deleting.** Read `engine.ts` around the team-turn dispatch. Confirm the structure is `if (actor.kind === 'team' && teamRuntimeById.has(actor.id)) { /* walked: runs runPlayerTurn */ } else if (actor.kind === 'team') { /* LEGACY — the branch to delete */ }`. Grep for any test that drives `runCombat` with a `kind:'team'` actor that has NO walked runtime (the audit found none — confirm). Confirm the helpers the branch calls — `synthesizeResisted`, `advanceChargeCadence`, `landsTimedEnemyApplication`, `bus.emit('skill-fired')` — are ALSO called from the focus/walked branches (they are; they MUST stay).

- [ ] **Step 2: Delete the `else if (actor.kind === 'team')` legacy branch** (the whole block, ~80 lines, including the `setLandsTimedEnemyApplication(...)` carve-out and its `sourceFired`/`synthesizeResisted` legacy usage). Do NOT delete the preceding walked-team `if` branch. Do NOT delete any shared helper.

- [ ] **Step 3: Verify byte-identity.** `npx tsc --noEmit` clean (catches any now-unused local/import — remove only if proven unused). `npx vitest run` → all green (same count). `git status --porcelain | grep '\.snap'` → empty. `npm run lint` → 0.

> If tsc flags a symbol as unused after deletion, confirm it was ONLY used by the legacy branch before removing it. If a test fails, the branch was NOT dead — STOP and report.

- [ ] **Step 4: Commit** — `git add -A && git commit -m "refactor(combat): A-sweep A.3 — delete dead legacy non-walked-team engine branch + sourceFired carve-out"`

---

## Task 2 (A.2): Collapse the live-OR-static debuff-landing fallback

**Files:**
- Modify: `src/utils/combat/effectiveStats.ts` (`liveDebuffLandingChance` ~119-131)
- Modify: `src/utils/combat/playerTurn.ts` (the `liveLandingComputable` ternary ~699-721)
- Modify: `src/utils/combat/engine.ts` (focus-attacker runtime `debuffLandingChance` field ~817/~1377 and its `?? debuffLandingChance` fallback at ~1314)
- Modify: `src/utils/combat/__tests__/triggers.test.ts` (4 fixture conversions)

- [ ] **Step 1: Write/extend a failing parity test** in `effectiveStats.test.ts`: `liveDebuffLandingChance` for a base-LESS actor (no `stats.hacking`/no defender `stats.security`), neutral affinity, no buffs, must equal the old static default `clamp(200 - 100, 0,100)/100 = 1.0`; and with a self Hacking Down −120 on a base-less attacker → `clamp((200−120) − 100,0,100)/100 = 0`. This locks the 200/100 default mechanism. Run `npx vitest run effectiveStats` → FAIL (current code yields `clamp(0 − 0)=0` for base-less / `(0−120)` etc., since effectiveStatsOf coerces base→0).

- [ ] **Step 2: Implement the 200/100 default INSIDE `liveDebuffLandingChance`.** Replace the `effectiveStatsOf(...).hacking`/`.security` reads with raw-base + folded-buff so a missing base defaults to 200/100 (NOT 0):

```typescript
export function liveDebuffLandingChance(
    statusEngine: StatusEngine,
    selfBuffLookup: Map<string, SelectedGameBuff[]>,
    attacker: CombatActor,
    defender: CombatActor,
    affinityDamageModifier: number
): number {
    const atk = foldActorBuffTotals(statusEngine, selfBuffLookup, attacker.id);
    const def = foldActorBuffTotals(statusEngine, selfBuffLookup, defender.id);
    const baseHacking = attacker.stats.hacking ?? 200;   // old static default
    const baseSecurity = defender.stats.security ?? 100;  // old static default
    const effHacking = (baseHacking + atk.hackingBuff) * (1 + affinityDamageModifier / 100);
    const effSec = baseSecurity + def.securityBuff;
    return Math.min(100, Math.max(0, effHacking - effSec)) / 100;
}
```
(`foldActorBuffTotals` is already exported from this module; this reproduces `effectiveStatsOf`'s hacking/security fold exactly but with the 200/100 base default. For a base-PRESENT actor it is identical to the prior `effectiveStatsOf(...).hacking` path.) Run `npx vitest run effectiveStats` → PASS.

- [ ] **Step 3: Make the live recompute unconditional in `runPlayerTurn`.** Remove the `liveLandingComputable` gate (`playerTurn.ts:699-721`); set `const liveLandingChance = liveDebuffLandingChance(statusEngine, selfBuffLookup, actor, enemy, affinityDamageModifier);` always. Remove the now-unused `debuffLandingChance` references that only fed the fallback ternary in this function.

- [ ] **Step 4: Retire the bare threaded-scalar fallback at the focus closure ONLY.** The single change here is `engine.ts:~1314`: the focus-attacker landing closure currently reads `attackerRuntime.liveDebuffLandingChance ?? debuffLandingChance` where the `?? debuffLandingChance` tail is the bare `runCombat` PARAM. Post-Step-3 the live field is always set, so change it to read `attackerRuntime.liveDebuffLandingChance` directly (drop the bare-param tail).
  - **DO NOT delete the `debuffLandingChance` field on the `PlayerActorRuntime` interface** (`playerTurn.ts:~156`): it is REQUIRED and SHARED — the enemy builder (`engine.ts:~509`), team builder (`~1462`), and the `triggers.ts:~932` DoT reader all use it. Leave the field; A.1 handles the triggers reader.
  - **KEEP** `:532` (`runtime.liveDebuffLandingChance ?? e.debuffLandingChance ?? 1`) and `:1446` (`runtime.liveDebuffLandingChance ?? w.debuffLandingChance`) — they fall back to the **retained public inputs** (enemy-attacker / walked-team), not the bare scalar.
  - **KEEP** the `debuffLandingChance` PARAM on `runCombat` / `CombatEngineInput.debuffLandingChance` / `enemyAttackers[].debuffLandingChance?` / `walk.debuffLandingChance` (public/derived inputs that still feed the enemy/team runtime fields and the `?? 200`/`?? 100` defaults indirectly). The bare `runCombat` `debuffLandingChance` param may become unused for the FOCUS path only after this change — if tsc flags it unused, it likely still feeds the focus runtime field set at `~1377`; verify before touching. Net: this step is a one-line closure edit, not a field/param removal.

> Goal: kill the focus closure's dependency on the bare static scalar (the silent dual-source). The shared runtime field + public inputs stay. Report exactly what you changed.

- [ ] **Step 5: Convert the 4 base-less `triggers.test.ts` fixtures.** The `debuffLandingChance: 0 / 0 / 0.5 / 0` cases on the base-less `baseInput` focus actor must instead supply `hacking`/`enemySecurity` on the input to reproduce the same chance under the live formula (neutral affinity assumed — verify each test's affinity): chance `0` → `hacking: 100, enemySecurity: 100` (clamp(0)=0); chance `0.5` → `hacking: 150, enemySecurity: 100` (clamp(50)/100=0.5). Apply per fixture; confirm each test's assertion (its lands/resists/DoT-landing outcome) is UNCHANGED. **DO NOT** touch `resistedEnemyDotsRoundEffects.test.ts` or similar — those pass `debuffLandingChance` on an **EnemyAttacker** input (a retained input), not a base-less focus actor.

> If you find MORE than these 4 base-less-focus fixtures, STOP and report the fuller list before converting (spec flagged this as the main risk).

- [ ] **Step 6: Verify byte-identity.** `npx vitest run` → all green. `git status --porcelain | grep '\.snap'` → empty. `npx tsc --noEmit` clean. `npm run lint` → 0.

- [ ] **Step 7: Commit** — `git add -A && git commit -m "refactor(combat): A-sweep A.2 — collapse live/static debuff-landing into one producer (200/100 default in liveDebuffLandingChance); convert 4 triggers fixtures (byte-identical)"`

---

## Task 3 (A.1): Unify the `triggers.ts` duplicate live-landing read

**Files:**
- Modify: `src/utils/combat/triggers.ts` (the DoT direct read ~932; the timed path ~896 calls the closure)

- [ ] **Step 1: Assess the post-A.2 landscape.** After A.2, every actor that takes a turn has `liveDebuffLandingChance` set (runPlayerTurn sets it). Read `triggers.ts:896` (timed: `owner.landsTimedEnemyApplication(cfg.application)` — closure, keeps the `'apply'`/`'inflict'` distinction) and `:932` (DoT: `owner.debuffLandingGate(owner.liveDebuffLandingChance ?? owner.debuffLandingChance)`). Determine whether `owner.liveDebuffLandingChance` is guaranteed set for every `owner` that can reach the DoT path (a DoT ticks during its owner's turn). If guaranteed, the `?? owner.debuffLandingChance` tail is dead.

- [ ] **Step 2: Extract one accessor + use it.** Add a small helper (e.g. `const actorLiveLandingChance = (owner) => owner.liveDebuffLandingChance ?? owner.debuffLandingChance;`) and call it from the DoT read (`:932`). The timed path keeps using its closure (which carries the `'apply'` logic) — A.1's goal is to remove the duplicated *direct field read*, not to merge the apply/inflict distinction. If Step 1 proved the `??` tail dead, the accessor can be `owner.liveDebuffLandingChance` with a comment; otherwise keep the `??` fallback in the single accessor.

> Keep it minimal and byte-identical. If `owner.debuffLandingChance` no longer exists on the relevant runtime type after A.2, the accessor is just `owner.liveDebuffLandingChance` — confirm via tsc.

- [ ] **Step 3: Verify byte-identity.** `npx vitest run triggers` + `npx vitest run` → green. Zero `.snap`. `npx tsc --noEmit` + `npm run lint` clean.

- [ ] **Step 4: Commit** — `git add -A && git commit -m "refactor(combat): A-sweep A.1 — unify triggers.ts live-landing read behind one accessor"`

---

## Task 4: Full gate + sub-project A closure

**Files:** none (verification only).

- [ ] **Step 1:** `npx vitest run` → all green.
- [ ] **Step 2:** `npm run lint` → 0 warnings.
- [ ] **Step 3:** `npx tsc --noEmit` → clean.
- [ ] **Step 4:** `npm run audit:skills` → 0 findings / 141 ships.
- [ ] **Step 5:** `git status --porcelain | grep -c '\.snap'` → `0` (only the 4 `triggers.test.ts` fixture-input edits changed in tests — confirm no snapshot moved).
- [ ] **Step 6:** `git status` → clean after the per-task commits.

---

## Done criteria (A-sweep)
- A.3: legacy non-walked-team branch + carve-out deleted; all shared helpers intact; byte-identical.
- A.2: `liveDebuffLandingChance` is the single landing-chance producer with the 200/100 base default; the `liveLandingComputable` ternary + the focus-attacker bare-scalar fallback are gone; enemy/walked retained inputs untouched; 4 `triggers.test.ts` fixtures converted to base-driven landing with unchanged assertions.
- A.1: one accessor for the triggers live-landing read; no duplicated direct read.
- No combat `.snap` moved; suite + lint + tsc + audit:skills clean.
- **Sub-project A is closed.** Next: sub-project B (Stasis).
