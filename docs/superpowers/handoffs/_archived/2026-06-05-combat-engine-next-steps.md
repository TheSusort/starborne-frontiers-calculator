# Handoff: Combat Engine — Next Steps after Phase 3

> Paste one of the prompt sections below into a fresh session. Context baseline: Phase 3
> ("reactive triggers") is MERGED to main as of 2026-06-05 — PR #83, 22 commits, including
> four game-rule/UX corrections from live user testing (persistent stacking statuses,
> Echoing Burst round visibility, turn-order strip filtering, Oleander semantics confirmed)
> and a five-finding CodeRabbit triage shipped on the PR. Memory file
> `project_combat_engine_roadmap.md` has the running state; `project_persistent_stacking_buffs.md`
> has the per-buff stacking rules.

## Current engine state (orient before any prompt)

- `src/utils/combat/`: `engine.ts` (~870 lines, turn-queue orchestrator + round accumulator +
  intent drains), `attackerTurn.ts` (~1040, the extracted attacker pipeline), `triggers.ts`
  (~350, cast/reactive partition + listeners + intent executor), `statusEngine.ts` (action-fed
  status machinery + persistent-stack maps), `shared.ts`, `state.ts`, `events.ts`,
  `abilityStatusGating.ts`. `src/constants/persistentStackingBuffs.ts` is the
  permanent-stacking override table (NOT buffs.ts — regenerated by fetch-buffs).
- Five LIVE triggers consumed end-to-end (`start-of-round`, `on-crit`, `on-debuff-inflicted`,
  `on-ally-debuff-inflicted`, `on-bomb-detonated`); `on-attacked`/`on-ally-destroyed`/
  `on-destroyed` annotation-only until enemy actions exist. Editor has a Trigger select.
- Golden parity suite: **18 snapshots** (`dpsGoldenParity.test.ts`) — the referee. Full suite
  1214 tests / 80 files.
- `docs/skill-model-coverage.md` §5 holds ALL game-verified rules (buff families, landing,
  persistent stacking, infliction-event definition, drain semantics); §6 backlog is current.

## Increment menu (recommended order)

| # | Increment | Size | Why next |
|---|---|---|---|
| 1 | **Team ShipSkills walk** (own spec) | ~Phase-2-sized | Unlocks team DoT lists, the ally-DoT trigger wiring (comment marks the spot in triggers.ts `on-ally-debuff-inflicted`), "ally inflicts Corrosion" ships, real team charge cadences from skills instead of hand-stamped buff lists. OPEN DESIGN PROBLEM: target-routing semantics for non-attacker sources (who does a team ship's damage/debuff hit? per-actor condition contexts? editor UI for team skills?). |
| 2 | **Extra End-of-Round Actions** (Nuqtu/Liberator/Chakara) | Small | The turn-count-agnostic round loop + `RoundAccumulator` (Phase 3 Task 2) is the ready seam — append attacker turns to the queue; row already tolerates 0..N attacker turns. Parser phrasings + queue append + row aggregation test. |
| 3 | **Per-hit crit checks** (backlog item 7, coverage doc §6) | Small-medium | User-confirmed: in-game each hit of a multi-hit skill crit-checks individually; our per-turn `roundCrit` undercounts on-crit trigger frequency ~3× for Enforcer-style ships. Needs per-hit accumulator draws + per-hit `ability-performed`; touches crit-multiplier aggregation in the damage math. |
| 4 | **Phase 4: full combat sim** | Large, own spec cycle | Enemy offensive actions (unlocks `on-attacked`, on-kill, cleanse consumption), targeting/taunt/provoke/stealth, multi-enemy, heal/shield/cleanse/control consumption, self-HP realism, own page, PvP/PvE round limits. |

---

## Prompt A — Team ShipSkills walk (full brainstorm → spec → plan flow)

Brainstorm and spec the team ShipSkills walk using the superpowers flow (brainstorming →
spec → spec review → user gate → writing-plans → subagent-driven implementation). Read first:

- `docs/superpowers/specs/2026-06-04-combat-engine-phase2-design.md` ("Team-ship depth"
  decision: re-timed buff lists were the deliberate stopgap; full walk = this spec) and
  `docs/superpowers/specs/2026-06-05-combat-engine-phase3-design.md` (listener/intent
  machinery the team walk plugs into).
- `src/utils/combat/engine.ts` team-turn block (currently: `sourceFired` + resisted
  staging only) and `statusEngine.ts` `teamSources`/`timedBySource` seams.
- `src/utils/combat/triggers.ts` — the `on-ally-debuff-inflicted` listener has a comment
  marking the dot-applied wiring point for when team DoTs exist.
- `docs/skill-model-coverage.md` §5–6; `docs/combat-system.md` §7–9 (targeting rules —
  the open problem's raw material).

Scope questions to triage during brainstorming:

1. **Target routing** (the open problem): a team ship's enemy-targeted abilities hit THE
   enemy dummy (single-enemy sim) — probably easy; its ally-targeted buffs: who receives
   them — the attacker only (current semantics), or modeled per-recipient? Self-buffs on a
   team ship affect... nothing DPS-visible unless they alter its cadence (speed/charge).
   Decide what's worth modeling vs annotating.
2. **Which ability types walk**: buff/debuff (replaces the hand-stamped SelectedGameBuff
   lists — no-double-count migration!), dot (NEW — team DoT lists, deferred since Phase 2),
   charge (team ships' own cadence already exists; charge GRANTS to the attacker would
   supersede `allyChargePerRound`?), damage (team damage contributes to enemy HP decline?
   — changes every HP-threshold gate; maybe out).
3. **Per-actor condition contexts**: a team ship's gated abilities need their own ctx
   (whose buffs count?). The drain-context builder pattern in triggers.ts is a reference.
4. **Reactive triggers on team ships**: do team ships' own reactive abilities register
   listeners (e.g. a team Hemlock gaining charges)? Or attacker-only this increment?
5. **Editor/UI**: team ship cards currently show buff/debuff pickers; does the skill
   editor open for team ships? Auto-fill from their ShipSkills?
6. **Migration**: `TeamActorInput.selfBuffs/enemyDebuffs` are the current API; additive
   evolution (e.g. optional `shipSkills`) vs replacement; the page's `buildSkillBuffAutoFill`
   path becomes redundant for team ships that walk.

## Prompt B — Extra End-of-Round Actions (lighter flow OK)

Spec and implement Extra End-of-Round Actions (Nuqtu's charged "grants itself 1 extra End
Of Round Action", Liberator, Chakara). Read the Phase 3 spec's Task-2 seam notes
(`RoundAccumulator`, 0..N attacker turns, `pendingResisted` routing) and
`docs/superpowers/plans/2026-06-05-combat-engine-phase3.md` Task 2. Survey the EoR
phrasings in docs/ship-skills.csv first. Decisions needed: when does the extra turn run
(immediately at end of round, after the enemy turn?); does it fire the active or charged
skill; does it bank charges; RoundData aggregation (the row sums damage across attacker
turns — verify the charges/action display reads sensibly with 2 turns).

---

## Hard constraints (unchanged — apply to every increment)

- DPS public API (`DPSSimulationInput`/`DPSSimulationResult`/`RoundData`) stays compatible
  (additive inputs fine).
- Zero-RNG determinism (`makeRateGate` accumulators; fixed listener/queue order).
- Golden parity (18 snapshots) is the referee — zero churn unless a documented KNOWN-DIFF,
  hand-verified; NEVER vitest `-u` (new scenarios self-write on first run; targeted
  regeneration = delete the entry + re-run).
- Listeners never mutate state; only the engine/executor mutates (Phase 1 contract).
- Pre-commit hook runs the full suite (~2 min); no `--no-verify` for code commits.
- docs/ is gitignored — `git add -f` for specs/plans/handoffs/coverage doc.
- **No RegExp lookbehind anywhere in src/** (production browserslist includes iOS Safari 15;
  CodeRabbit Critical on PR #83 — use the `splitSentences`/`matchesActiveSelfCrit` patterns
  in skillTextParser.ts).
- Changelog: ONE evolving DPS entry in `UNRELEASED_CHANGES` (user directive) — edit it in
  place, no new per-PR DPS entries until the combat-engine phases are done.

## Game-verified rules (do NOT re-litigate; all implemented)

See `docs/skill-model-coverage.md` §5 for the full set. Highlights added 2026-06-05:

- **Persistent stacking statuses** (buff-NAME overrides skill-text duration): Defense Shred
  (max 20) / Blast (4) / Overload (10, until-kill ≈ permanent in-sim) / Titanite Plating
  (5, −1/incoming-hit ≈ permanent in-sim) — `persistentStackingBuffs.ts`. Defense Matrix is
  genuinely timed. Warding Screen UNVERIFIED (open question).
- Infliction events: discrete landed applications + DoT entries; family-blocked-but-landed
  COUNTS; recurring/aura standing effects do NOT.
- Charge-on-inflict = +1 per infliction event; ally variant requires a real ally infliction
  (attacker's own don't count); per-standing scaling stays for "per buff/debuff ON the
  target" texts.
- A triggered effect never boosts the action that triggered it (drain after turn body);
  drain-time HP% includes round-so-far damage (post-hit), unlike the attacker's own gates
  (entering-round — deliberate convention split, documented at the drain call site).
- **User-accepted divergence (backlog 7)**: on-crit fires once per crit TURN; in-game
  multi-hit skills crit per hit — trigger frequency undercounts for multi-hit ships.

## Workflow

Full superpowers flow for Prompt A (brainstorming one question at a time — the scope list
above is the question list → spec in `docs/superpowers/specs/` → spec-reviewer loop → user
gate → writing-plans → subagent-driven-development on a `feat/` branch). Prompt B can run a
lighter spec. Golden discipline per task; two-stage review per task; live verification with
the user's imported fleet at the end (dev server localhost:3002, chrome MCP tools; fleet in
that origin's localStorage — 212 ships; pages with 200+ ships exceed snapshot token limits,
use evaluate_script). Watch for CodeRabbit triage on the PR before merging (Phase 3 had 5
findings; triage → fix/skip-with-reason → reply in-thread → wait for its re-review status
check → merge when clean; merge commits, branches kept).
